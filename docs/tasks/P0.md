# P0 · 平台、来源、模型与日历主通道验证

> 唯一主要目标：**在写任何业务代码之前，确认这套方案依赖的四件外部事实是否成立。**
> 依据：主方案 §2.2—§2.4、§3.1、§3.5、§6.2、§10.4。

## 阶段性质：这一阶段大部分不是编码

真实 Cloudflare 账户资格、真实发信、真实账单、真实日历客户端，执行者 Agent 无权也无法完成。Agent 的交付是：**只读探测代码、采集脚本、证据登记模板、结论表骨架，以及一份"需所有者执行的操作清单"**。

三条铁律：

1. **不得用模拟结果填充 P0 证据。**探针跑不通就如实记录跑不通。
2. **不得开通任何收费资源、不得发送真实邮件、不得升级计划。**需要这些时写进"需所有者执行"清单。
3. **P0 未完成不等于可以跳过。**"先把账号中心做完"不是放行替代（§10.4）。P1/P2/F1/F2 可并行推进，但 P3 之后的放行受 P0 结论约束。

---

### P0-01 · 能力探针骨架（只读，不开通资源）

| 项 | 内容 |
| --- | --- |
| 前置 | — |
| 允许改动范围 | `scripts/probes/**`、`docs/evidence/**`（新建） |
| 继承合同 | §2.1、§2.4、§3.1 |
| 验收 ID | A-P0-PROBE |

**交付物**

- `scripts/probes/` 下一组独立可跑的只读探针，每个探针输出结构化 JSON 到 `docs/evidence/p0/<probe>-<时间戳>.json`：
  - `sources-reachability`：目标环境对四个官方来源的可达性、状态码、响应大小、是否出现鉴权/验证码/访问限制。
  - `d1-conditional-tx`：D1 条件事务与 `batch` 的真实行为，特别是 **CAS 更新零行时 batch 是否回滚**（§3.6、§8.1）。
  - `do-send-location`：DO 的发送位置与 alarm 行为观测。
  - `model-echo`：向已批准的模型 profile 提交一个**固定的合成样本**，记录完整 usage 字段名与数值（不做质量评估，只确认能拿到哪些计费字段）。
- `docs/evidence/p0/README.md`：证据文件命名规范、每个字段的含义、哪些字段属于秘密不得提交。
- `docs/evidence/p0/OWNER_CHECKLIST.md`：需所有者执行的操作清单（开通、授权、账单查询、真实收件人测试、客户端安装）。

**关键约束**

- 探针**只读**。出现鉴权、验证码、访问限制时记录并退出，**不实施绕过、不伪装 UA、不使用第三方聚合后端冒充官方来源**（§3.1）。
- 请求必须限制域名、重定向、超时、大小与类型；不提供公开通用抓取代理。
- 模型探针只用合成样本，样本文件显式标 `synthetic: true`。

**不在范围**：任何业务表、任何写操作、任何发信。

**回退点**：`scripts/probes/` 与 `docs/evidence/` 可整体删除，不影响其他目录。

---

### P0-02 · 官方来源真实样本采集与来源注册项登记

| 项 | 内容 |
| --- | --- |
| 前置 | P0-01 |
| 允许改动范围 | `scripts/probes/**`、`fixtures/sources/**`、`docs/evidence/p0/**` |
| 继承合同 | §3.1、§3.2、附录 A.1 的 `SOURCE_*`、附录 B |
| 验收 ID | A-P0-SOURCE（门禁 G-P0-SOURCE） |

**交付物**

- 每个来源的真实样本存入 `fixtures/sources/<source_id>/`：列表响应、正文响应、分页边界、置顶项、含图片承载日期的样本、跨年/纯日期/结束包含性歧义的样本各至少一份。真实样本标 `synthetic: false` 并记录抓取时间与 URL。
- `fixtures/sources/registry.draft.json`：每个来源的 `source_id / game / region / adapter / approved_hosts / verified_publishers / cursor / poll_policy / verification_state`。
- `SOURCE_LIMIT_PROFILE` 的实测值：页数、正文大小、请求超时、重定向与批量上限。
- 一份"列表展示时间 ≠ 活动时间"的实证对照（至少三例）。

**关键约束**

- 后三个公告来源的列表/正文方法为 `getAnnList` / `getAnnContent`；**不得把列表展示时间当成活动时间**（§3.1）。
- 生产发布者 UID、栏目、区域与分页以本次保存的真实样本为准登记，不沿用原稿线索当结论。
- 样本中若含用户可识别信息，采集脚本必须剥离后再入库。

**不在范围**：适配器实现、抽取、入库。

---

### P0-03 · 模型抽取质量与计费基线

| 项 | 内容 |
| --- | --- |
| 前置 | P0-02 |
| 允许改动范围 | `scripts/probes/**`、`fixtures/eval/**`、`docs/evidence/p0/**` |
| 继承合同 | §3.4、§3.5、附录 A.3 的 `AI_*` 与 `MODEL_*` |
| 验收 ID | A-P0-MODEL（门禁 G-P0-MODEL） |

**交付物**

- 标注集：`EXTRACTION_EVAL_MIN = 120` 篇，三游戏均覆盖，按时间/版本隔离调优集与保留集。合成边界样本单独目录并标 `synthetic`。
- 计费基线报告，逐项给出分布（中位数与高分位）：输入 token、**完整计费输出**（不是可见 JSON 大小）、推理相关 usage、截断率、结构修复率、网络重试率、延迟、单篇费用。
- `MODEL_MAX_INPUT` 与 `MODEL_MAX_BILLED_OUTPUT` 的建议值及依据；**无法确定完整计费输出上界时，明确写"不开放自动调用"**。
- 质量报告：精确时间错误、关键事件召回（门槛 `KEY_EVENT_RECALL ≥ 95%`）、自动发布比例、审核比例、媒体缺口。

**关键约束**

- `@cf/qwen/qwen3-30b-a3b-fp8` 是**测试对象**，不是已确认适合本项目的结论。
- **不得用可见 JSON 大小代替供应商计费输出**；不得猜测部署是否支持关闭思考参数。
- 经济 profile 未同时通过质量与费用测试前保持未启用。
- 保留集的已知时间错误门槛是**有限样本门槛**，报告中不得写成"生产零错误"。

**不在范围**：抽取管线实现、候选审核界面。

---

### P0-04 · 日历客户端完整快照与 VALARM 实测

| 项 | 内容 |
| --- | --- |
| 前置 | P0-01 |
| 允许改动范围 | `scripts/probes/**`、`fixtures/ics/**`、`docs/evidence/p0/**` |
| 继承合同 | §1.2、§6.2、§6.4、§6.5、§10.3 客户端条目、[R11] |
| 验收 ID | A-P0-CAL（门禁 G-P0-CAL） |
| **状态** | **Agent 侧交付物已就绪**：`scripts/probes/calendar-clients/serve.mjs` + `docs/evidence/p0/calendar-clients.md`。本卡剩余部分是所有者实测，Agent 不再领取 |

**交付物**

- 一组手工构造的 ICS 序列（`fixtures/ics/`），按时间顺序模拟：初始集合 → 移除一条 → 再加入同 UID → 改期到更高 SEQUENCE → 越过未来窗口 → 输出 CANCELLED → 恢复确定日期。含中文与 emoji 折行、纯日期、DISPLAY VALARM 样本。
- 一个可由所有者本地运行的静态托管脚本，把上述序列按步骤提供给订阅 URL。
- `docs/evidence/p0/calendar-clients.md`：**每个客户端单独一行**，记录客户端名称与版本、操作系统、网络、每一步的实测结果、刷新延迟、VALARM 是否触发。

**关键约束**

- **Apple Calendar、Google Calendar、Outlook 不共用一份"支持"结论**；完整替换、VALARM、刷新延迟分别评级（§10.3）。
- 未通过删除/重新加入测试的客户端标为"未支持或功能受限"，不提供自动筛选同步承诺。
- 若**没有任何客户端通过**，在结论表中写明"须重新评估日历主通道"，**不得偷偷恢复个人撤销池**（§6.2）。

**不在范围**：个人 Feed 服务端实现。

---

### P0-05 · 邮件发送资格与反馈链路实测

| 项 | 内容 |
| --- | --- |
| 前置 | P0-01 |
| 允许改动范围 | `scripts/probes/**`、`docs/evidence/p0/**` |
| 继承合同 | §2.2、§2.3、§2.4、§7.4—§7.7、[R01]—[R06] |
| 验收 ID | A-P0-MAIL（门禁 G-P0-MAIL） |
| **状态** | **所有者步骤文档已就绪**：`docs/evidence/p0/EMAIL_SETUP_RUNBOOK.md` + `platform-facts.md`。剩余的 MailProvider 接口草案并入 P4-03，Agent 不再单独领取本卡 |

**交付物**

- 所有者执行步骤文档：发件子域 DNS 配置（认证域与业务域分开）、关闭认证域 Email preview、两个用途均关闭"静默丢弃受抑制收件人"。
- 实测记录：Paid/Beta 资格、站点与发件域、**账户日发信权限**（= `PLATFORM_MAIL_DAY_LIMIT`）。账单周期与周期包含量在本项目不适用（ADR-0003）。
- **面向普通收件人**（授权的 QQ/163/Gmail/Outlook 等，非 routing verified destination）的发送记录：messageId、反馈 Queue 事件、收件服务器接受口径。
- MailProvider 接口草案：返回 `accepted(messageId) / rejected / unknown`，**不得只返回 boolean**。

**关键约束**

- **给 routing verified destination 发成功，不能证明 Email Sending 生命周期反馈链成立**（§2.4）。
- 不得用真实用户群发做压测。
- 实际日发送权限随账户情况变化，**不能从月包含量推导**。

**不在范围**：调度、预算、退订实现。

---

### P0-06 · P0 结论表与放行决策记录

| 项 | 内容 |
| --- | --- |
| 前置 | P0-02、P0-03、P0-04、P0-05 |
| 允许改动范围 | `docs/evidence/p0/**`、`docs/adr/**` |
| 继承合同 | §2.3、§10.4 |
| 验收 ID | A-P0-GATE |

**交付物**

- `docs/evidence/p0/CONCLUSIONS.md`：逐项 **通过 / 失败 / 未取得**，每项附证据文件路径与取得时间。
- 失败项的明确决策：继续、降级、改方案。改方案的写成 ADR。
- 门禁状态更新：`docs/BUILD_PLAN.md` 第 2 节的 G-P0-* 逐条标记开/未开。

**关键约束**

- **"未取得"不等于"通过"。**结论表里不允许出现空格或"待补"。
- 若 G-P0-CAL 未开，实现可以继续，但对外不得出现"日历提醒可用"的表述（§1.2）。
