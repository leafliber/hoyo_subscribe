{
  "name": "@hoyo/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.22.0",
    "typescript": "7.0.2",
    "vitest": "4.1.11",
    "wrangler": "4.136.0"
  },
  "dependencies": {
    "@hoyo/contracts": "workspace:*"
  }
}
