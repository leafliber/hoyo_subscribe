# AGENTS.md — 执行者 Agent 工作合同

> 适用范围：本仓库全部实现工作。每次任务开始前**完整读完本文件**，再读所属阶段的任务卡，然后才动手。
> 本文件只指向当前版本合同：主方案 v2.1（r2）与前端设计 v1.0（r2）。不存在其他有效版本。

## 0. 项目一句话

米哈游官方日程订阅服务：**公开浏览 + 邮箱验证码账号 + 云端订阅配置 + 固定个人 ICS（可启用日历提醒）+ 有预算的邮件补充 + 可选 Web Push**。目标部署形态是一个 Cloudflare Workers 项目、一个 D1、两个固定 DO。

## 1. 文档权威顺序

| 顺序 | 文档 | 地位 |
| --- | --- | --- |
| 1 | `docs/HOYO_OFFICIAL_EVENT_SUBSCRIPTION_PLAN_v2.1.md` | **合同**。业务、数据、安全、预算、生命周期、放行条件的唯一来源 |
| 2 | `docs/HOYO_SUBSCRIPTION_FRONTEND_DESIGN_v1.0.md` | **合同**。页面组织、交互、前端验收 |
| 3 | `docs/CONTRACTS_BASELINE.md` | 导航摘要。跨阶段反复用到的公式与枚举的集中索引 |
| 4 | `docs/ENGINEERING.md` | 工程约定：仓库、工具链、测试分层、DoD |
| 5 | `docs/BUILD_PLAN.md` | 阶段依赖、门禁、任务卡总表 |
| 6 | `docs/tasks/<阶段>.md` | 当前这一张任务卡 |
| 7 | `docs/ACCEPTANCE.md` | 验收矩阵与测试 ID 映射 |

**只有 1 和 2 是合同。**3–7 是为了让你不必每次通读 160 KB 原文而做的导航层；导航层与合同冲突时，以合同为准，并同时提交一个修正导航层的 PR。

## 2. 十条硬规则

1. **一次只交付一张任务卡。**不顺手扩范围、不顺手重构相邻模块、不为"反正要改"提前动别的阶段。
2. **参数只有一个来源。**所有阈值、TTL、配额、提前量来自主方案附录 A 导出的配置注册表（`packages/contracts`）。正文、模块、前端、文案里**不得出现第二份字面常量**。发现需要新参数：先加注册表并补 A.5 依赖等式校验，再使用。
3. **业务规则只有一个定义源。**枚举、规范化、提醒规则表、投影语义写在 `packages/contracts` 的纯函数里，Worker 与 Web 共同消费。不得按中文标签反推规则，不得在前端另写一套判断。
4. **未执行的测试不得写成通过。**只写实际跑过的命令与实际输出。模拟的、跳过的、失败的必须逐条标明。"代码没报错"不是验收证据。
5. **不擅自开通收费资源、不发真实邮件、不连生产发送链、不升级计划、不新增供应商、不扩大权限。**需要这些时停下来，在交付报告里列成"需所有者执行的前置"。
6. **不绕过官方访问控制。**来源站点出现鉴权、验证码或访问限制时，停用该来源并标为维护，不实施绕过、不换 UA 伪装、不用第三方聚合后端冒充官方来源。
7. **秘密不进仓库、不进日志、不进前端产物、不进遥测、不进截图。**包括 Cookie、OTP、恢复码、完整邮箱、Feed/退订 URL、Push endpoint 与密钥。
8. **合同冲突或做不到时，停手并写 ADR。**不自行改动业务含义来让实现通过。ADR 模板见 `docs/adr/0000-TEMPLATE.md`。
9. **遵守第 3 节禁止清单。**这些不是风格偏好，是上一版被明确推翻的设计。
10. **交付报告按第 5 节模板写。**缺证据的交付按未完成处理。

## 3. 禁止清单（实现层面的红线）

来自主方案 §10.5 以及 r2 修订。命中任何一条即判定不合格，须回退重做：

- 恢复"个人撤销池"（`feed_retirements`）、筛选差集暂存、逐用户取消记录。
- 恢复全局日历取号（`calendar_change_no`）或把已废弃的 `removal_change_no` 混入 SEQUENCE 公式。
- 让提醒发送资格隐式与 `calendar.node_types` 相交（"隐式三重筛选"）。
- 把变更通知（`new_event` / `important_change` / `cancelled_or_retracted` / `late_discovery`）重新挂回 `rule_ids`，或以"规则为空"为由拒绝保存。
- 未经浏览器确认的 pending 会话自动挤占 active 名额，或自动淘汰最早的会话。
- 让旧退订链接返回成功却不实际关闭当前业务邮件。
- 让**紧急停用消费恢复码**。
- 用 `floor(R / D)` 取代带 `carry` 与 `E=1` 兜底的 envelope。
- 给新账号写入用户没选过的默认配置（`DEFAULT_*` 只是界面预选）。
- 去掉个人 Feed 输出前的**完整快照缩水守卫**。
- 要求用户专门回网页点一次才能续邮件席位。
- 把公共浏览"可展示陈旧缓存"的策略移植成私人 Feed 的成功兜底。
- 把 `accepted` 当成"已送达"或"已读"。
- 在认证邮件里夹带业务退订头或日程推广。

## 4. 工作流

```text
读 AGENTS.md → 读 BUILD_PLAN 确认门禁已开 → 读任务卡 → 确认前置任务卡已验收
  → 建分支 → 先写合同/类型与测试骨架 → 实现 → 跑全部命令 → 写交付报告 → 提 PR
```

- **分支**：`<阶段>/<任务卡 ID>-<短描述>`，例如 `p1/P1-03-param-registry`。
- **提交**：一个逻辑改动一个提交，信息首行用中文祈使句说明做了什么。
- **PR**：一张任务卡一个 PR，标题以任务卡 ID 开头。PR 描述即交付报告。
- **门禁**：前置任务卡未验收通过时，不得开始依赖它的任务卡；阶段门禁见 `docs/BUILD_PLAN.md` 第 2 节。

## 5. 交付报告模板（PR 描述必须包含全部小节）

```markdown
## 任务卡
<ID> · <标题> · 阶段 <P/F编号>

## 改动范围
实际改动的路径清单；与任务卡"允许改动范围"的差异及原因。

## 继承的合同
本次实现依据的主方案/前端文档章节号；实现中对合同的理解如有推断，逐条列出。

## 交付物
逐项对照任务卡"交付物"，说明完成/未完成。

## 执行过的命令与结果
逐条粘贴实际命令与真实输出摘要（含失败）。未执行的写"未执行"，不留空、不写"应该通过"。

## 验收测试
| 验收 ID | 测试文件:用例名 | 实际结果 |
未覆盖的验收 ID 单独列出并说明原因。

## 证据
截图/日志/样本文件路径。真实外部依赖（账户、发信、客户端）产生的证据需标明获取方式与时间；无法获取的标为"需所有者执行"。

## 不在本次范围
明确列出任务卡中标注为"不在范围"的内容，确认未被顺手实现。

## 已知问题与回退点
未解决问题；回退到哪个提交可恢复上一可用状态。
```

## 6. 验收方会检查什么

交付后由验收方（另一 Agent 或所有者）复核，重点是：

1. **禁止清单命中检查**——直接搜代码与迁移文件。
2. **参数单一来源检查**——搜索字面量魔法常量、搜索前端是否复制了 `MAIL_*` / `SESSION_*` 等值。
3. **测试真实性检查**——重跑你报告里的命令，对比输出；报告说通过而实际失败的，整张卡退回。
4. **验收 ID 覆盖检查**——对照 `docs/ACCEPTANCE.md`，未覆盖且未说明的退回。
5. **范围检查**——超出"允许改动范围"的文件需要在报告里有理由。
6. **合同一致性抽查**——挑 2–3 条公式（UID/SEQUENCE、envelope、变更通知范围、缩水守卫）核对实现。

## 7. 现在可以做什么

见 `docs/BUILD_PLAN.md`。简述：

- **P0** 大部分需要所有者的真实账户与设备，Agent 只交付探测代码、采集脚本、证据模板与结论表，不代替决策。
- **P1、P2、F1、F2** 可在隔离环境推进，不依赖 P0 的收费能力结论。
- **P3 及之后**受 P0 放行结论约束：日历主通道未取得至少一个客户端的实测通过，不得开放"日历提醒可用"的版本。
