// packages/contracts：Worker 与 Web 共用的唯一业务定义源
// （枚举、Schema、参数注册表、时间语义、规范化纯函数——docs/ENGINEERING.md §1）。
// P1-01 仅建立包骨架；内容由 P1-02 起的任务卡填充（docs/tasks/P1.md）。
// 依赖版本在本卡固定（zod 见 package.json）；此处不得出现任何第二份参数常量或业务规则。
export {};
